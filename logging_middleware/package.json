{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Centralized type-safe logging middleware for Campus Notifications System",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
